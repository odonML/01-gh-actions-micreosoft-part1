#.github/actions/docker-tool/action.yml
name: 'Docker Tool'
description: 'Corre un script dentro de un contenedor Linux'
runs:
  using: 'docker'
  image: 'Dockerfile' # GitHub buscará el Dockerfile en esta misma carpeta